26 января 9Б
1. Приветствие (кого нет, заполнение СГО)
2. Знакомство. Каждый говорит своё имя и откдуда он приехал
3. Правила ТБ

пароль: student
меняем раскладку CTRL + SPACE

Работа с папками (параллельно с граф.):
0. Список файлов и папок ls -l
1. Зайти в папку cd название папки
2. Выход из папки cd ..
3. Создание папки mkdir название папки
4. Удаление папки rm -r название папки
5. Адрес до текущей папки pwd
6. Переход по адресу cd адрес
7. Создать файл touch название_файла

Задания:

С помощью терминала:
1. Создать папку на рабочему столе с названием technology9a
2. Внутри этой папки создать txt файлы c названиями ВСЕХ цифр на английском: например one.txt и так далее
3. Вывести в терминал список всех созданных файлов с помощью утилиты ls
4. Открыть в инкогнито сайт blockly.ru и пройти до 5-го уровня черепашки

23.03

Цикл — это конструкция, которая позволяет выполнять один и тот же блок кода несколько раз подряд. В Python самый популярный цикл для таких задач — это `for`.

for i in range(КОЛИЧЕСТВО_ПОВТОРОВ):
    # Здесь пишем команды, которые нужно повторить
    # Эти команды обязательно должны быть с отступом (tab)
    t.forward(100)
    t.left(90)

- for — ключевое слово, начало цикла
- i — это переменная-счетчик
- in range(n) — задает диапазон повторений от 0 до n-1
- Двоеточие (:) в конце строки с `for` обязательно
- Отступ (4 пробела) перед командами внутри цикла обязателен

Цикл внутри цикла

for i in range(n):  # внешний цикл
    # команды внешнего цикла
    for j in range(m):  # внутренний цикл
        # команды внутреннего цикла

Два квадрата

import turtle

t = turtle.Turtle()

for j in range(2):
  for i in range(4):
    t.forward(25)
    t.left(90)
  t.forward(25)

turtle.done()